# Домашнее задание: Обработка исключений

## Задание
Реализовать обработку исключений в классах интернет-магазина.

## Выполненные требования

### 1. Обработка исключений при создании товаров
- ✅ Валидация количества товара (quantity > 0)
- ✅ Исключение `ValueError` при попытке создать товар с quantity ≤ 0
- ✅ Четкие сообщения об ошибках

### 2. Обработка исключений при работе с категориями
- ✅ Проверка типа добавляемого объекта
- ✅ Исключение `TypeError` при попытке добавить не-продукт в категорию
- ✅ Корректная обработка пустых категорий
### 3. Загрузка данных из JSON
- ✅ Функция `load_categories_from_json()` для загрузки данных
- ✅ Обработка ошибок: файл не найден, некорректный JSON, отсутствие полей
- ✅ Безопасное создание объектов

## Структура проекта

### Основные классы (`src/models.py`):
- `BaseProduct` - базовый класс товара с валидацией
- `Product` - обычный товар
- `Smartphone` - смартфон (наследник Product)
- `LawnGrass` - газонная трава (наследник Product)
- `Category` - категория товаров с методами:
  - `add_product()` - добавление товара с проверкой типа
  - `calculate_average_price()` - расчет средней цены
  - `__len__()` - общее количество товаров
- `LoggingMixin` - миксин для логирования операций
- `load_categories_from_json()` - функция загрузки данных из JSON

### Демонстрационная программа (`src/main.py`):
- Создание примерных данных
- Демонстрация обработки исключений
- Вывод информации о магазине
## Как запустить

### Основная демонстрация:
```bash
python src/main.py
# Ручные тесты
python test_simple.py
python test_json.py

# Автоматические тесты (если установлен pytest)
python -m pytest tests/ -v
json
{
  "categories": [
    {
      "name": "Электроника",
      "description": "Электронные устройства",
      "products": [
        {
          "name": "Смартфон",
          "description": "Мощный смартфон",
          "price": 29999.99,
          "quantity": 10
        }
      ]
    }
  ]
}
from src.models import Product, Category

# Создание товара с валидацией
try:
    product = Product("Ноутбук", "Игровой", 89999, 5)
except ValueError as e:
    print(f"Ошибка: {e}")

# Работа с категорией
category = Category("Электроника", "Техника")
category.add_product(product)
# Попытка добавить не-продукт (вызовет исключение)
try:
    category.add_product("не продукт")
except TypeError as e:
    print(f"Ошибка типа: {e}")

# Расчет средней цены
print(f"Средняя цена: {category.calculate_average_price():.2f} руб.")
def __init__(self, name, description, price, quantity):
    if quantity <= 0:
        raise ValueError(f"Количество товара должно быть больше 0. Получено: {quantity}")
def add_product(self, product):
    if not isinstance(product, (Product, Smartphone, LawnGrass)):
        raise TypeError(f"Ожидается объект Product, получен {type(product)}")
def load_categories_from_json(file_path: str):
    try:
        # Проверка существования файла
        # Чтение и парсинг JSON
        # Построение объектов
    except (FileNotFoundError, JSONDecodeError, KeyError) as e:
        # Логирование ошибки и возврат пустого списка
        return []
✅ Все требования задания выполнены
✅ Код готов к проверке
# 🛒 Домашнее задание: Обработка исключений в интернет-магазине
## 📋 Задание
Реализовать обработку исключений в классах интернет-магазина для безопасной работы с данными.

## ✅ Выполненные требования

### 1. Обработка исключений при создании товаров
- ✅ Валидация количества товара (quantity > 0)
- ✅ Исключение `ValueError` при quantity ≤ 0
- ✅ Четкие сообщения об ошибках в исключениях
### 2. Обработка исключений при работе с категориями
- ✅ Проверка типа добавляемого объекта
- ✅ Исключение `TypeError` при добавлении не-продукта
- ✅ Корректная обработка пустых категорий

### 3. Загрузка данных из JSON
- ✅ Функция `load_categories_from_json()` для загрузки данных
- ✅ Обработка ошибок: файл не найден, некорректный JSON
- ✅ Безопасное создание объектов из внешних данных

## 📁 Структура проекта
online_store_project/
├── src/
│ ├── models.py # Основные классы и функции
│ ├── main.py # Демонстрационная программа
│ └── init.py
├── tests/
│ ├── test_json_loading.py # Тесты загрузки JSON
│ ├── test_main.py # Тесты основной программы
│ └── init.py
├── test_data.json # Пример данных в JSON формате
├── test_simple.py # Ручные тесты
├── test_json.py # Тест JSON загрузки
├── requirements.txt # Зависимости проекта
└── README.md # Этот файл

## 🚀 Как запустить

### Основная демонстрация:
```bash
python src/main.py
Тестирование:
bash
# Ручные тесты
python test_simple.py
python test_json.py

# Автоматические тесты (если установлен pytest)
python -m pytest tests/ -v
📊 Пример вывода основной программы
text
============================================================
ОНОЛАЙН-МАГАЗИН: Демонстрация обработки исключений
============================================================

1. Попытка создать товар с quantity = 0:
✅ УСПЕХ: Исключение обработано: Количество товара должно быть больше 0...

2. Попытка добавить строку вместо продукта:
✅ УСПЕХ: Исключение обработано: Ожидается объект Product...

3. Средняя цена пустой категории:
Результат: 0.0 (должно быть 0)
💻 Пример использования
python
from src.models import Product, Category

# Создание товара с валидацией
try:
    product = Product("Ноутбук", "Игровой", 89999, 5)  # Успешно
    bad_product = Product("Битый", "Товар", 100, 0)    # Вызовет исключение
except ValueError as e:
    print(f"Ошибка валидации: {e}")

# Работа с категорией
category = Category("Электроника", "Техника")
category.add_product(product)

# Попытка добавить не-продукт
try:
    category.add_product("строка")  # Вызовет исключение
except TypeError as e:
    print(f"Ошибка типа: {e}")
# Расчет средней цены
print(f"Средняя цена: {category.calculate_average_price():.2f} руб.")
🔧 Ключевые моменты реализации
1. Валидация в BaseProduct:
python
def __init__(self, name, description, price, quantity):
    if quantity <= 0:
        raise ValueError(f"Количество товара должно быть больше 0. Получено: {quantity}")
2. Проверка типа в Category:
python
def add_product(self, product):
    if not isinstance(product, (Product, Smartphone, LawnGrass)):
        raise TypeError(f"Ожидается объект Product, получен {type(product)}")
3. Безопасная загрузка JSON:
python
def load_categories_from_json(file_path: str):
    try:
        # Проверка существования файла
        # Чтение JSON
        # Создание объектов
    except (FileNotFoundError, JSONDecodeError, KeyError) as e:
        print(f"Ошибка загрузки: {e}")
        return []  # Возвращаем пустой список при ошибке
🧪 Тестовые данные (test_data.json)
json
{
  "categories": [
    {
      "name": "Электроника",
      "description": "Электронные устройства",
      "products": [
        {
          "name": "Смартфон",
          "description": "Мощный смартфон",
          "price": 29999.99,
          "quantity": 10
        },
        {
          "name": "Ноутбук",
          "description": "Игровой ноутбук",
          "price": 89999.99,
          "quantity": 5
        }
      ]
    }
  ]
}
🎯 Результат
✅ Все требования задания выполнены
✅ Код отлажен и протестирован
✅ Документация создана
✅ Проект готов к проверке
Домашнее задание выполнено в рамках курса по Python
