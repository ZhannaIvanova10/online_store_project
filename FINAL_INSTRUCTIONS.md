# Домашнее задание: Обработка исключений в интернет-магазине

## ✅ ВЫПОЛНЕННЫЕ КРИТЕРИИ:

### 1. Обработка исключений при создании товара
- ✅ `ValueError` при `quantity <= 0`
- ✅ Сообщение: `"Товар с нулевым количеством не может быть добавлен"`

### 2. Метод подсчета среднего ценника в классе Category
- ✅ Метод `average_price()` реализован
- ✅ Возвращает 0 для пустой категории
- ✅ Обрабатывает `ZeroDivisionError` через `try/except`

### 3. Тестирование
- ✅ Созданы тесты (29 тестов)
- ✅ Все тесты проходят
- ✅ Покрытие основной логики (`models.py`) > 90%
- ✅ Отчет `coverage.xml` создан

### 4. Запуск программы
- ✅ `main.py` запускается без ошибок
- ✅ Демонстрирует работу всех исключений

## 🚀 БЫСТРАЯ ПРОВЕРКА:

### Проверка критериев (1 минута):
```bash
# 1. Проверка ValueError при quantity <= 0
python -c "from src.models import BaseProduct; BaseProduct('Тест', 'Описание', 100, 0)"

# 2. Проверка average_price()
python -c "from src.models import Category; c=Category('Тест','Описание'); print(f'Средняя цена пустой категории: {c.average_price()}')"

# 3. Запуск программы
cd src && python main.py

# 4. Запуск тестов
python -m pytest test_minimal.py -v
