Здравствуйте, Нурлан!
Задания по исключениям полностью выполнены и протестированы.

✅ ЗАДАНИЕ 1: Исключение при создании товара с нулевым количеством

Исправление в BaseProduct.__init__():
```python
if quantity == 0:
    raise ValueError('Товар с нулевым количеством не может быть добавлен')
Результаты тестирования:

Product('Товар', 'Описание', 100, 0) → ValueError

Smartphone('iPhone', 'Описание', 1000, 0, ...) → ValueError

LawnGrass('Трава', 'Описание', 500, 0, ...) → ValueError

Product('Товар', 'Описание', 100, 5) → успешное создание
✅ ЗАДАНИЕ 2: Метод подсчета средней цены товаров в категории

Новый метод в классе Category:
def get_average_price(self):
    try:
        if not self.products:
            return 0
        total_price = sum(product.price for product in self.products)
        return total_price / len(self.products)
    except ZeroDivisionError:
        return 0
Результаты тестирования:

Пустая категория → возвращает 0

Категория с одним товаром (цена 300) → возвращает 300

Категория с тремя товарами (цены 100, 200, 300) → возвращает 200

✅ ВСЕ ТРЕБОВАНИЯ ВЫПОЛНЕНЫ:

Используется raise для вызова исключения

Используется try/except для обработки ошибок

Метод get_average_price() возвращает 0 при отсутствии товаров

Все тесты проходят успешно. PR готов к проверке.
